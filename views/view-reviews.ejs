<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Reviews - <%= manuscript.title %></title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to external CSS file -->
    <!-- If you're not using an external file, you can include the styles directly in the HTML -->
    <style>
        /* General Styles */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1;
            padding: 20px;
            width: 85%;
            max-width: 800px;
            margin: 100px auto 20px; /* Top margin for spacing under header */
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #004c97;
            text-align: center;
            margin-bottom: 20px;
        }

        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #004c97;
            color: white;
            font-weight: bold;
        }

        td {
            color: #333;
            font-size: 14px;
        }

        tr:nth-child(even) {
            background-color: #f0f0f0;
        }

        /* No Reviews Message */
        p {
            text-align: center;
            font-size: 16px;
            color: #666;
        }

        /* Header and Footer Styles */
        header, footer {
            background-color: #002c5f;
            color: white;
            text-align: center;
            padding: 20px 0;
            position: fixed;
            width: 100%;
            z-index: 1000;
        }

        header {
            top: 0;
        }

        footer {
            bottom: 0;
        }

        /* Media Queries */
        @media screen and (max-width: 768px) {
            main {
                width: 95%;
            }

            table {
                font-size: 14px;
            }

            th, td {
                padding: 8px;
            }

            p {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <main>
        <h2>Reviews for "<%= manuscript.title %>"</h2>
        <% if (reviews.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>Reviewer</th>
                        <th>Feedback</th>
                        <th>Decision</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    <% reviews.forEach(function(review) { %>
                        <tr>
                            <td>Anonymous</td> <!-- Or include reviewer's ID if anonymity is not required -->
                            <td><%= review.feedback %></td>
                            <td><%= review.decision %></td>
                            <td><%= new Date(review.review_date).toLocaleDateString() %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p>No reviews have been submitted for this manuscript yet.</p>
        <% } %>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
